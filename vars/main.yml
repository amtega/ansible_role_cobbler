---
# Role variables

# Packages required by cobbler service

cobbler_packages:
  - cobbler
  - debmirror
  - httpd
  - tftp-server

# Base directory to temporary mount ISOs

cobbler_iso_mount_dir: "{{ cobbler_iso_dir }}/mounted"
